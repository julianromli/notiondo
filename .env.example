# libSQL / Turso (use Turso Cloud URL + token in production, or file: for local)
DATABASE_URL=file:./.data/local.db
# DATABASE_AUTH_TOKEN=

# better-auth
BETTER_AUTH_SECRET=generate-a-long-random-string
BETTER_AUTH_URL=http://localhost:3000
